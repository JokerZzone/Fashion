<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanou.dao.CartMapper">
	
	<select id="selectByUserId" resultMap="CartResultMap.cartResult">
		select * from cart 
			where user_id = #{userId}  and order_id = 0
	</select>
	
	<select id="selectByGoodsId" resultMap="CartResultMap.cartResult">
		select * from cart where goods_id = #{goodsId} and order_id = 0
	</select>
	
	<select id="selectMax" resultType="Integer">
		select max(parent_id) from cart 
			where user_id = #{userId}
	</select>
	
	<insert id="addCart">
		insert into cart(user_id,session_id,goods_id,
			goods_name,shop_price,goods_number) values
			(#{userId},#{sessionId},#{goodsId},#{goodsName},#{shopPrice},#{goodsNumber})
	</insert>
  
  	<update id="updateCart">
		update cart set goods_number=#{goodsNumber}
		where goods_id=#{goodsId} and user_id=#{userId} and order_id = 0
	</update>
	
	<update id="updateGoodsNum" parameterType="int">
		update cart set goods_number=#{1} where rec_id = #{0}  and order_id = 0
	</update>
	
	<delete id="deleteAllCartByUId" parameterType="int">
		delete from cart where user_id = #{user_id} and order_id = 0
	</delete>
	
	<delete id="deleteCartById" parameterType="int">
		delete from cart where rec_id = #{rec_id} and order_id = 0
	</delete>
	
	<update id="updateOrder_id" parameterType="int">
		update cart set order_id = #{1} where user_id = #{0}  and order_id = 0
	</update>
	
	<select id="findOrder_Carts" parameterType="java.util.Map" resultMap="CartResultMap.cartResult">
		select * from cart where user_id = #{user_id} and order_id = #{order_id}
	</select>
</mapper>