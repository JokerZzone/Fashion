<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanou.dao.GoodsGalleryMapper">

	<select id="findImgPre" resultType="String">
		select img_pre from goods_gallery 
			where goods_id = #{0}
	</select>
	
	<select id="findLeft" resultType="String">
		select goods_img from goods_gallery 
			where goods_id = #{0} and type = 0
	</select>
	
	<select id="findDetail" resultType="String">
		select goods_img from goods_gallery 
			where goods_id = #{0} and type = 1
	</select>
	
	<select id="findImgSuff" resultType="String">
		select img_suff from goods_gallery 
			where goods_id = #{0}
	</select>
	
	<update id="updateImgDesc">
		update goods_gallery set img_desc = #{0}
			where img_id = #{1}
	</update>
	
</mapper>